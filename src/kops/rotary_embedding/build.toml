[general]
name = "rotary-embedding"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "aymuos15/rotary-embedding"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.rotary_embedding]
backend = "cuda"
depends = ["torch"]
src = ["csrc/rotary_embedding.cu"]
