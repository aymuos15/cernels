[general]
name = "deform-attn"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/deform-attn"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.deform_attn]
backend = "cuda"
depends = ["torch"]
src = ["csrc/deform_attn.cu"]
