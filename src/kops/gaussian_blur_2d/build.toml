[general]
name = "gaussian-blur-2d"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "aymuos15/gaussian-blur-2d"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.gaussian_blur_2d]
backend = "cuda"
depends = ["torch"]
src = ["csrc/gaussian_blur_2d.cu"]
