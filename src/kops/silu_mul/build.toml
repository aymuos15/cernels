[general]
name = "silu-mul"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/silu-mul"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.silu_mul]
backend = "cuda"
depends = ["torch"]
src = ["csrc/silu_mul.cu"]
