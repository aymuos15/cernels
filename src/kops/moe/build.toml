[general]
name = "moe"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/moe"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.moe]
backend = "cuda"
depends = ["torch"]
src = ["csrc/moe.cu"]
