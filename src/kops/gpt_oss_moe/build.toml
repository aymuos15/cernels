[general]
name = "gpt-oss-moe"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/gpt-oss-moe"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.gpt_oss_moe]
backend = "cuda"
depends = ["torch"]
src = ["csrc/gpt_oss_moe.cu"]
