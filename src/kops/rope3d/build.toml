[general]
name = "rope3d"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/rope3d"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.rope3d]
backend = "cuda"
depends = ["torch"]
src = ["csrc/rope3d.cu"]
