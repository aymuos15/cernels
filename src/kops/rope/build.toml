[general]
name = "rope"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/rope"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.rope]
backend = "cuda"
depends = ["torch"]
src = ["csrc/rope.cu"]
