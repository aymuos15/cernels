[general]
name = "sam-decomposed-rel-pos"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/sam-decomposed-rel-pos"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.sam_decomposed_rel_pos]
backend = "cuda"
depends = ["torch"]
src = ["csrc/sam_decomposed_rel_pos.cu"]
