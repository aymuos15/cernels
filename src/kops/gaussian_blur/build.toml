[general]
name = "gaussian-blur"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/gaussian-blur"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.gaussian_blur]
backend = "cuda"
depends = ["torch"]
src = ["csrc/gaussian_blur.cu"]
