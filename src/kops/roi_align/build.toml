[general]
name = "roi-align"
version = 1
license = "Apache-2.0"
backends = ["cuda"]

[general.hub]
repo-id = "kernels-local/roi-align"

[torch]
src = [
    "torch-ext/torch_binding.cpp",
    "torch-ext/torch_binding.h",
]

[kernel.roi_align]
backend = "cuda"
depends = ["torch"]
src = ["csrc/roi_align.cu"]
