# dataset

Build and browse a dataset of all [HF Kernel Hub](https://huggingface.co/models?filter=kernels) repos.

```bash
uv run --no-sync python src/dataset/build.py   # build kernels.parquet
uv run --no-sync python src/dataset/view.py    # browse it (interactive)
```
